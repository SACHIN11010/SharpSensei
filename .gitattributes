# Normalize all text files to LF in the repository and in the working tree on
# every platform. The browser bundle (js/bundle.js) is a generated artifact that
# CI re-builds and diff-checks against the committed copy, so its line endings
# must be deterministic regardless of a contributor's core.autocrlf setting.
* text=auto eol=lf
